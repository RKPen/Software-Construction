/**
 * Calculates the factorial of a non-negative integer.
 *
 * @param n The non-negative integer for which to calculate the factorial.
 * @return The factorial of n.
 */
int factorial(int n) {
    // Implementation 
}


/**
 * Calculates the binomial coefficient (n choose k) for two non-negative integers n and k.
 *
 * @param n The total number of items.
 * @param k The number of items to choose from the total.
 * @return The binomial coefficient (n choose k).
 */
int binomialCoefficient(int n, int k) {
    // Implementation 
}

/**
 * Generates and prints a row of Pascal's Triangle for a given row index.
 *
 * @param rowIndex The row index for which to generate and print the row of Pascal's Triangle.
 */
void generatePascalsTriangle(int rowIndex) {
    // Implementation 
}

/**
 * The main function prompts the user to enter a row index, generates and prints
 * the corresponding row of Pascal's Triangle.
 *
 * @return 0 on successful execution.
 */
int main() {
    // Implementation 
}
